# 企业智能文档问答系统 · 架构文档 v1.1

**版本**：v1.1（2026-08-27）　**密级**：内部　**定位**：个人独立开发、可落地、可给真实用户使用的 RAG 文档问答系统
**上游文档**：《企业智能文档问答系统-项目方案文档 v2.0.0》（企业级参考）、《个人版优化报告 v1.0→v1.1》（选型决策依据）

**已锁定决定（决定后续一切）：**
- **部署形态（W6）= A（本机自用/演示）+ B（局域网给身边人用）**
- **LLM 路线 = 混合**：本地跑 bge 嵌入/重排（GPU）+ LLM 生成走**云端 API**（无 GPU）
- **硬件 = RTX 4050 笔记本（6GB 显存）**

---

## 1. 项目概述

面向企业/团队内部非结构化文档（技术规范、手册、制度、项目文档）的**私有化语义问答**。用户上传文档，系统自动解析、向量化、索引；用户用自然语言提问，系统基于**检索增强生成（RAG）**给出**带引用溯源**的答案。数据不出本机/局域网，可私有化部署。

**与 v2.0 的关系**：v2.0 是 7 人团队、10 周、¥2.4万/月的企业级标杆（含 GraphRAG/多模态/Agentic/持续评估）。本 v1.1 是**单人可交付**的可落地版本——继承其"质量优先、可验证"的骨架，**主动砍掉**超出单人范围的旗舰功能（见 §2 非目标），并以"能真正上线给身边人用"为第一约束。

---

## 2. 目标与非目标

### 2.1 目标（MUST，W6 前必须实现）
- 支持 PDF/Word/Excel/Markdown/图片 上传 → 解析 → 语义问答
- 混合检索（向量 + BM25 + RRF）+ 重排
- 答案**带可靠引用**（no source → no claim）
- 轻量**多用户登录 + 检索时权限过滤**
- **用户反馈闭环**（点赞/点踩/纠错 → 喂回评估）
- **per-query 全链路 trace** + 调试面板
- **本机 A + 局域网 B**：`docker-compose up`，绑 `0.0.0.0`，防火墙放行，身边人局域网访问

### 2.2 非目标（WON'T —— 明确不做的，避免过度设计）
- ❌ GraphRAG / 知识图谱（除非有实测的"多跳/关系型"失败模式）
- ❌ 多模态 RAG（图片/图表视觉向量）—— 除非知识确有相当比例是图片
- ❌ Agentic 路由 + MCP 工具调用
- ❌ 多租户 SaaS、企业级 SSO、微服务拆分
- ❌ 本地 LLM（6G 显存跑不动像样的本地大模型）
- ❌ Celery + Redis broker、Milvus 全家桶（单机轻量替代）

> **设计原则**：核心链路**去框架化**（原生 SDK），仅参考 LangGraph 的编排思想；用**最简工具**解决当前规模；把功夫花在**文档解析质量**和**"能给人用"的支柱**上，"可扩展性"留在文档/适配层里讲清楚，不当场硬堆。

---

## 3. 总体架构（分层）

```
┌─────────────────────────────────────────────────────────────┐
│ 前端层  React 18 + Ant Design + SSE 流式 + Markdown 引用渲染  │
│        文件上传 / 知识库管理 / 对话 / 反馈 / 调试面板          │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTP / SSE
┌──────────────────────────▼──────────────────────────────────┐
│ 应用层  FastAPI (无状态, 可水平扩)                            │
│  ├─ 文档上传 API   ├─ 问答 API (SSE)  ├─ 知识库/会话/反馈 API  │
│  └─ 调试/评估 API   └─ 鉴权 (JWT/RBAC 轻量版)                 │
└──────────┬──────────────┬──────────────┬────────────────────┘
           │              │              │
   ┌───────▼────────┐ ┌───▼──────────┐ ┌─▼────────────────┐
   │ RAG 核心引擎     │ │ 异步任务       │ │ 评估与调试        │
   │ (问题优化→检索→  │ │ (APScheduler  │ │ (黄金集/RAGAS/    │
   │  重排→生成)     │ │  + PG 任务表)  │ │  trace/引用校验)  │
   └───────┬────────┘ └──────────────┘ └──────────────────┘
           │
┌──────────▼──────────────────────────────────────────────────┐
│ 检索层  混合检索: pgvector(HNSW 向量) + PostgreSQL(FTS/BM25)   │
│         → RRF 融合 → bge-reranker-large 重排 → 阈值过滤        │
└──────────┬──────────────────────────────────────────────────┘
           │
┌──────────▼──────────────────────────────────────────────────┐
│ 模型层  (混合)                                                │
│  ├─ 本地 GPU(4050 6G): bge-large-zh 嵌入 + bge-reranker-large │
│  ├─ 本地 CPU:          Docling/PP-Structure 解析(异步)        │
│  └─ 云端 API:          LLM 生成 (DeepSeek / SiliconFlow)      │
└──────────┬──────────────────────────────────────────────────┘
           │
┌──────────▼──────────────────────────────────────────────────┐
│ 存储层  PostgreSQL(pgvector, 关系+向量+BM25 一套) + Redis       │
│        (会话/限频/embedding 缓存) + 文件系统(原始文件/解析缓存)  │
└─────────────────────────────────────────────────────────────┘
Infra: Docker Compose 一键起 (单机)
```

---

## 4. 核心组件与技术选型

| 层级 | 技术 | 版本/型号 | 选型理由 |
|---|---|---|---|
| 后端 | FastAPI + Uvicorn | 0.110+ | 异步原生、自动 OpenAPI |
| 数据库+向量 | PostgreSQL + **pgvector** | 16+ | 一套存关系+向量+BM25，零新增基础设施（**替代 Milvus**） |
| 缓存 | Redis | 7+ | 会话/限频/embedding 缓存 |
| 任务调度 | **APScheduler + PG 任务表** | — | **替代 Celery**（单机足够，少一堆服务） |
| 嵌入（本地 GPU） | BAAI/bge-large-zh-v1.5 | fp16 | 中文强，0.3B 6G 显存轻松 |
| 重排（本地 GPU） | BAAI/bge-reranker-large | fp16 | 精排最高 ROI（本地跑），约 2-3G |
| LLM（云端 API） | DeepSeek / SiliconFlow qwen | API | **6G 跑不动本地大模型** → 走 API 保稳定 |
| 文档解析 | PyMuPDF + python-docx + openpyxl + Docling(+PP-Structure) | — | 原生文字走 PyMuPDF，表格/扫描件走 Docling/PP-Structure（见 §5） |
| 前端 | React 18 + Ant Design | 18+ | 企业级 UI，SSE 流式渲染 |
| 部署 | Docker Compose | — | 一键起，面试可复现；K8s 只写清单不真搭 |

---

## 5. 文档处理管线（Ingestion，异步）

```
用户上传(分片,断点续传) → 存原始文件(文件系统) → 入 PG 任务表(状态=pending)
        ↓ APScheduler 触发
解析路由(按文档"性质"分派):
  ├─ 原生文字 PDF/Word/Excel → PyMuPDF / python-docx / openpyxl (最快)
  ├─ 表格/复杂版面/扫描件   → Docling / PP-StructureV3 (表格结构/OCR)
  └─ 统一输出结构化 Markdown
        ↓
语义分块 (Parent-Child) + 上下文摘要(每 chunk 前置来源摘要)
        ↓
嵌入向量化 (bge-large-zh, 本机 GPU) → 向量入库 pgvector
        ↓
元数据写入 PG (chunk 级: user/kb/doc/version/parent_id/page…) → 状态=indexed
```

**要点：**
- **解析路由**是 RAG 质量天花板（借鉴 RAGFlow DeepDoc 思路）——普通文档快，复杂文档准。
- **解析在 CPU 异步跑**，失败重试；不阻塞问答。
- 解析产物（Markdown + 表格结构 + 源片段）缓存到文件系统，便于重新分块/重排向量。

---

## 6. 问答管线（Query，同步）

```
用户提问
  ↓
问题优化(多轮指代消解 + 查询改写)  [借鉴 FastGPT]
  ↓
混合检索(并行):
  ├─ 向量检索: pgvector HNSW, 标量过滤 expr="user/role/kb"
  ├─ 关键词检索: PostgreSQL FTS(BM25)
  └─ RRF 融合 → Top-20
  ↓
重排 bge-reranker-large (本机 GPU) → 相关性分数
  ↓
阈值过滤(用真实数据校准,不拍脑袋) → 取 Top-5
  ↓
LLM 生成(云端 API) + 引用标注(带稳定 chunk ID 的原文片段)
  ↓
后处理: 引用校验(no source → no claim) + 敏感词过滤 + 置信度
  ↓
打点(反馈/评估) → SSE 流式返回
```

**要点：**
- **权限在检索时过滤**（服务端注入 user/role/kb 到向量检索表达式），不是答案生成后再判断——参考 Dify，避免信息泄漏。
- **引用校验**是防幻觉关键：每句论断必须能追溯到带稳定 ID 的引用，否则明说"据现有资料无法确定"。
- **阈值用真实数据校准**：先用 30–50 条查询画出相关性分数分布再定，避免"系统变神经质"。

---

## 7. 部署架构（A + B + RTX 4050 6G）

### 7.1 单机拓扑

```
[你的 4050 笔记本, Windows]
  ├─ Docker Compose: FastAPI / Postgres+pgvector / Redis / 前端(静态)
  ├─ 本机 GPU: bge-large-zh 嵌入 + bge-reranker-large 重排
  ├─ 本机 CPU: Docling/PP-Structure 解析(异步)
  └─ 云端 API: LLM 生成
      ↓ 绑 0.0.0.0:8000
[局域网内身边人] http://<你内网IP>:8000  ← 同一 WiFi 下访问
```

### 7.2 硬件预算（4050 6G）

| 组件 | 显存/算力 | 说明 |
|---|---|---|
| bge-large-zh 嵌入 | fp16 ≈ 0.7G | 轻松 |
| bge-reranker-large 重排 | fp16 ≈ 2-3G（含激活） | 小批并发够，总计约 3-4G 留余量 |
| 本地 LLM | **≥6G 不可行** | → 选云端 API 的硬原因 |
| 解析 | CPU 异步 | 慢没关系，不阻塞 |
| LLM 生成 | 0（云端） | 无 GPU |

**规模：几个并发**（身边人）。轻量基建正好，不上 Celery/Milvus。

### 7.3 A+B 上线要点
- 后端绑 **`0.0.0.0`**（非 `127.0.0.1`）。
- 身边人用 `http://<内网IP>:8000`。
- **Windows 防火墙放行端口**（入站规则）——最常卡住的点。
- 至少**轻量登录**（账号 + 每个用户可见知识库），衔接"检索时权限过滤"。
- 机器需在线才能提供局域网服务（B 的固有约束，可接受）。

---

## 8. 数据模型（PostgreSQL + pgvector）

```sql
-- 用户（轻量 RBAC：admin / uploader / viewer）
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  username VARCHAR(64) UNIQUE, password_hash TEXT, role VARCHAR(16),
  created_at TIMESTAMPTZ DEFAULT now()
);

-- 知识库
CREATE TABLE knowledge_bases (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  owner_id UUID REFERENCES users(id), name VARCHAR(128), description TEXT,
  embedding_model VARCHAR(64) DEFAULT 'bge-large-zh',
  created_at TIMESTAMPTZ DEFAULT now()
);

-- 文档
CREATE TABLE documents (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  kb_id UUID REFERENCES knowledge_bases(id),
  filename VARCHAR(256), file_path VARCHAR(512), version INT DEFAULT 1,
  status VARCHAR(32) DEFAULT 'pending',  -- pending/processing/indexed/failed
  page_count INT, chunk_count INT DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT now()
);

-- 文档分块（含父子关联 + 权限元数据 + 全文检索向量）
CREATE TABLE document_chunks (
  id VARCHAR(64) PRIMARY KEY,          -- 稳定 chunk id（用于引用）
  parent_id VARCHAR(64),
  kb_id UUID REFERENCES knowledge_bases(id),
  doc_id UUID REFERENCES documents(id),
  owner_id UUID,                       -- 检索时权限过滤
  content TEXT NOT NULL,               -- child: 检索用
  parent_content TEXT,                 -- parent: 生成用
  page_num INT, chunk_type VARCHAR(16),-- parent/child
  search_vector TSVECTOR,              -- BM25
  created_at TIMESTAMPTZ DEFAULT now()
);
-- pgvector 向量列（单列存向量 + 元数据列支持标量过滤）
ALTER TABLE document_chunks ADD COLUMN embedding vector(1024);
CREATE INDEX ON document_chunks USING hnsw (embedding vector_cosine_ops);
CREATE INDEX idx_chunks_owner_kb ON document_chunks(owner_id, kb_id);
```

**关键**：chunk 级**元数据齐全**（owner/kb/doc/version/parent_id/page）——因为**向量相似度本身无法判断权限与文档新鲜度**，必须用元数据在检索时过滤。

---

## 9. 权限与多用户

- 鉴权：JWT（轻量 RBAC），角色从登录身份**服务端**派生，不从请求体/前端传。
- **检索时过滤**：检索 `expr` 注入 `owner_id`/`kb_id` 等过滤器，**而非生成后再判断**。
- 引用与权限耦合：**答案不能引用用户无权看到的文档**（引用校验同时校验权限）。
- 错误路径也不泄漏：即使内容被过滤，系统行为也要一致。

---

## 10. 质量与可靠性支柱（"能给人用"的关键）

| 支柱 | 实现 | 指标/作用 |
|---|---|---|
| **引用校验** | no source → no claim；每论断可追溯稳定 chunk ID；展示源片段原文 | 引用覆盖率，防幻觉 |
| **用户反馈闭环** | 点赞/点踩/纠错 → 落库 → 上黄金集 → 每周抽坏例重评 | 用户侧"真相"，驱动迭代 |
| **可观测/调试** | per-query 全链路 trace（chunk id/分数/prompt/回复/反馈）+ 调试面板"给我看检索中间结果" | 快速定位解析/检索/生成问题 |
| **降级策略** | LLM 失败/超时 → 语义缓存命中 → 纯检索返回 Top-3 原文 → "服务暂不可用" | 可用性兜底 |

---

## 11. 关键设计决策与取舍

| 决策 | 选择 | 理由（面试可讲） |
|---|---|---|
| 向量库 | pgvector（不硬堆 Milvus） | Milvus 为亿级/分布式设计，单机版要 etcd+MinIO，**比当前问题更重**；做**向量存储适配层**，README 写 Milvus/Qdrant 迁移与集群预案，规模上来平滑切换 |
| 任务队列 | APScheduler + PG 任务表（不用 Celery） | Celery 是 80% 过度设计（"我有笔记本和梦想"）；单人+单机用最简工具，留换 Celery 的抽象层 |
| LLM | 本地嵌入/重排 + 云端 API 生成（混合） | **4050 6G 跑不动本地大模型**；本地小模型拿"本地部署"分，生成走 API 保稳定 |
| 框架 | 核心链路去框架化，仅编排参考 LangGraph | 高频路径原生 SDK，讲得透、可控、避免依赖地狱 |
| GraphRAG/多模态/Agentic | 砍掉 | 95% 企业查询用确定 Modular RAG 就够；graph 成本/延迟/维护不成比例；多模态运维最重。除非实测有对应失败模式 |

---

## 12. API 设计（关键端点）

```http
POST /api/v1/documents            # 上传(分片) → 返回 doc_id, status=processing
GET  /api/v1/documents/{id}       # 查处理状态
POST /api/v1/chat/stream          # SSE 流式问答 {kb_id, question, session_id}
POST /api/v1/chat/{msg_id}/feedback  # 点赞/点踩/纠错
GET  /api/v1/debug/query?q=...    # 调试:返回检索中间结果(chunk/分数/重排)
GET  /api/v1/metrics/ragas        # 评估指标(趋势)
POST /api/v1/auth/login           # JWT 登录
```

---

## 13. 目录结构（建议）

```
enterprise-rag/
├── backend/
│   ├── app/
│   │   ├── main.py  config.py
│   │   ├── api/v1/{auth,documents,chat,knowledge,feedback,debug,metrics}.py
│   │   ├── core/
│   │   │   ├── parser.py         # 解析路由 (PyMuPDF/Docling/PP-Structure)
│   │   │   ├── chunker.py        # Parent-Child + 上下文摘要
│   │   │   ├── embedding.py      # bge-large-zh (GPU)
│   │   │   ├── retriever.py      # pgvector + BM25 + RRF
│   │   │   ├── reranker.py       # bge-reranker-large (GPU)
│   │   │   ├── generator.py      # 云端 API LLM + 引用
│   │   │   └── vector_store.py   # 向量存储适配层 (pgvector/Milvus/Qdrant 可切)
│   │   └── services/{document_service,chat_service}.py
│   ├── tasks/{document_tasks.py} # APScheduler + PG 任务表
│   ├── tests/  requirements.txt  Dockerfile
├── frontend/src/{components,pages,services}
├── deploy/{docker-compose.yml, init.sql}
├── docs/{architecture.md, evaluation.md}
└── README.md
```

---

## 14. 评估与监控

- **黄金测试集**（50-100 条真实查询 + 期望答案/来源）——**先建**，不最后建。
- RAGAS（或轻量自评）：Faithfulness / Context Recall / Answer Relevancy / **引用覆盖率**。
- CI 卡点：任一指标较基线**下降 >3% 阻断发布**。
- 每周从点踩样本抽坏例，重评估，圈定"解析/检索/生成"哪一环退化。
- 监控：p50/p90/p99 延迟、检索命中率、引用覆盖率、幻觉率、降级率。

---

## 15. 演进路径（可扩展性，文档里讲清、不当场实现）

1. **向量库**：pgvector → Qdrant（10M-100M、低延迟/过滤重）→ Milvus（100M+ 分布式/GPU 索引）——走 `vector_store.py` 适配层。
2. **任务队列**：APScheduler → Celery + Redis（真需异步重试/优先级时）。
3. **本地 LLM**：换 ≥16G 显存后，把生成切回本地（模型路由已留接口）。
4. **部署**：Docker Compose → K8s（Deployment+Service+HPA 清单已备，扩展预案）。
5. **多租户**：chunk 元数据已有 owner/kb，加 tenant 后升级为多租户 + 数据库层面隔离。
6. **更高阶 RAG**：仅在实测出现明确失败模式时，爬 ladder（GraphRAG/多模态/Agentic）。

---

## 16. 风险与对策

| 风险 | 应对 |
|---|---|
| 文档解析质量（表格/扫描件）差 | 解析路由 + Docling/PP-Structure；解析失败告警；用真实文档做体检 |
| 引用不靠谱（幻觉） | 引用校验 + no source → no claim + 引用覆盖率指标 + 黄金集 |
| 6G 显存瓶颈（本地小模型） | 混合：生成走 API；嵌入/重排 fp16；解析走 CPU 异步 |
| 阈值/检索退化 | 用真实数据校准阈值；退化告警（Context Recall 连续低于阈值） |
| 本地机器私密性（B 模式） | 局域网 + 轻量登录 + 检索时权限过滤 + 防火墙只对内网放行 |
| 个人/团队文档安全 | 数据不出机；敏感词过滤 + PII 脱敏；审计日志 |

---

*本文档为 v1.1 工程级目标架构，基于已锁定决定（A+B 部署、混合 LLM、RTX 4050 6G）。所有"已做"均须以真实实现与验证为准，写在简历/面试的每个数字要先真跑出来再写。*
