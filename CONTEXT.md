# CONTEXT — 领域词汇表

> 本文件只放**领域术语**(这个项目里这些词的确切含义),不放实现细节、不放待办。术语在这里定了,就在所有地方(issue / 测试名 / 提案)沿用,不要换同义词。

## 实体
- **知识库 (Knowledge Base)** — 一组文档的逻辑集合,按 owner 隔离。
- **文档 (Document)** — 上传进知识库的原始文件;有解析状态(indexed / failed 等)。
- **分块 (Chunk)** — 文档切出的检索/生成单元。项目区分:**子块 (child)** 供检索(更聚焦)、**父块 (parent)** 供生成(上下文更全)。
- **会话 (Session) / 消息 (Message)** — 一次多轮问答;消息带角色(用户/助手)与来源。

## 检索
- **混合检索 (Hybrid Retrieval)** — 向量检索与关键词检索(BM25)并用后融合。
- **重排 (Rerank)** — 对召回候选按与问题的相关性做二次排序。
- **命中率 (Hit@k)** — 正确分块是否出现在检索结果前 k 条。
- **召回率 (Recall@k)** — 被找回的正确分块占全部正确分块的比例。
- **MRR** — 首个正确分块名次的倒数(排序质量)。

## 引用与忠实
- **引用 (Citation)** — 答案中标注论断的来源(文档名 + 页码)。
- **no source → no claim** — 没有可引用来源时,不得凭模型自身知识作断言。
- **忠实度 (Faithfulness)** — 答案的论断是否真的被引用来源支撑(抗幻觉)。
- **引用覆盖率 (Citation Coverage)** — 被来源支撑的论断占比。
- **拒答 (Refusal / Negative Rejection)** — 来源不含答案时,明确回答“无法确定”,而非编造。

## 评测
- **黄金集 (Golden Set)** — 人工标注的问答评测集;每条 = 问题 + 期望事实,可选 页码 / 负样本 / 所属文档。
- **负样本 (Negative Sample)** — 答案不在文档中的问题;期望系统**拒答**。
- **正样本 (Positive Sample)** — 黄金集里**要查事实**的条目(与负样本相对);只有正样本进事实 / 页码指标的分母。
- **能力 (Ability)** — 按「考什么维度」给黄金集条目分组(如 RGB 的噪声鲁棒 / 否定拒绝 / 信息集成 / 反事实鲁棒);
  报告按能力分别出数字。代码里对应条目的 `group` 字段。

## 代理与工具
- **工具 (Tool)** — 代理可以调用的一个能力单元;自带 MCP 规范的 `name` / `description` / `inputSchema`。
  **实现只有一份**,两种消费方式:进程内直调、经 MCP server 对外暴露。
- **MCP (Model Context Protocol)** — 工具对外暴露与互操作的协议;本项目的 server 默认走本地 stdio。
- **代理链路 (Agent Path)** — 代理（ReAct 循环 + 工具）构成的**可选**问答链路；由**代理开关**控制，
  **默认关**，关闭时行为与确定性单步管线完全一致。
- **评测适配器 (Eval Adapter)** — 把某条问答链路包成评测核心认的那一个缝（问题 -> 答案 + 来源），
  使不同链路**同口径可比**（评测核心不改）。
- **双轨评测** — 领域黄金集(自建 → 业务数字)+ 公开基准 RGB(→ 业界可比 / 拒答数字)。

## 自带 Key（BYOK）
- **自带 Key (BYOK)** — 用户填入自己的 LLM 供应商配置，由系统按其调用；未配置时回落服务端全局模型。
- **LLM 配置** — 一份自带 Key 的配置内容：`base_url` / `api_key` / `model`。
- **服务端全局模型** — 由服务端统一配置、所有人共用的那一个 LLM；未配置自带 Key 时的回落目标。
- **按请求解析** — 每次问答按**发起用户**决定用哪个 LLM：有自带配置用自带，否则用全局。
- **能力探测** — 探测某模型具备哪些能力（如是否支持工具调用）；探测不到时按**保守默认**处理。

## 上下文与记忆
- **轮 (Turn)** — 一组「用户提问 + 助手回答」。**消息 (Message)** 是单条，因此一轮 = 2 条消息 —— 两者不可混称。
- **上下文预算** — 读取侧允许带入的历史规模（按 token 计）；与生成侧的**输出上限**（`llm_max_tokens`）是两个独立概念，不混用。
- **对话摘要** — 把**更早**的对话压成的一段短文；压缩时它替代原文，最近若干轮仍保留原文。
- **滚动摘要与游标** — 摘要**落在会话上**、随对话推进增量更新；游标记「已摘要到哪条消息」，只滚动它之后的未摘要尾部（票 18）。
- **事实 (Fact)** — 用户在对话中**明确告知**、可跨会话复用的信息（身份/角色、偏好、业务背景、约定的口径）。文档内容与助手自己的回答**不算**事实。
- **跨会话记忆 (Memory)** — 按用户隔离的**事实**集合；**不可作为引用来源** —— 回答的依据仍必须来自文档。
