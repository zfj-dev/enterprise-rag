# 17: 上下文预算装配 + token 计数缝

**What to build:** 给上下文装配加一层预算控制——给定历史与预算，**未超预算时原样透传**（零额外开销），超了才压缩并保留最近若干轮原文。token 计数能力从外部注入，使测试确定性、无网络。

**Blocked by:** None (can start immediately).

**Status:** ready-for-agent

- [ ] 未超预算时**原样透传**：不触发压缩、不产生额外 LLM 调用
- [ ] 超预算时触发压缩，且**保留最近若干轮原文**（保证指代消解仍有近期上下文）
- [ ] 预算与"输出上限"是两个独立概念，不混用
- [ ] token 计数能力从外部注入；stub 计数下断言**确定、无网络、无真实 LLM**
