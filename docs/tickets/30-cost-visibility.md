# 30: 成本可见性（管理员全局 / 用户自己）

**What to build:** **管理员看全局聚合**（总额、按用户、按窗口），**普通用户只看自己**；复用已有指标接口与 per-query trace，不新起观测通道。

**Blocked by:** 27 per-query 用量记账；28 费用折算 + 价格表

**Status:** ready-for-agent

- [ ] 管理员可看到全局聚合（总额、按用户、按窗口）
- [ ] 普通用户**只能看到自己**的用量，看不到他人
- [ ] 成本挂在**已有的 per-query trace** 上（排障时一处看全）
- [ ] 复用**已有指标接口**作为入口，不新开一套
- [ ] 演示/Fake 模式下同样成立（Fake 也有 token 与单价）
