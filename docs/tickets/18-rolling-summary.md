# 18: 滚动摘要落库 + 增量复用

**What to build:** 超出预算的更早对话压成一段**落在会话上的滚动摘要**，随会话推进增量更新；刷新或重启后，长对话的上下文**不回到起点**。

**Blocked by:** 17 上下文预算装配 + token 计数缝

**Status:** ready-for-agent

- [ ] 摘要**持久化在会话上**，并记录"已摘要到哪"的游标
- [ ] 摘要**只在未摘要尾部再次超预算时**重算一次，之后复用（用 stub 摘要器的调用次数断言增量复用）
- [ ] 刷新页面 / 重启服务后，长对话的上下文仍在（不回到起点）
- [ ] 摘要器从外部注入；默认 Fake 返回固定摘要，保证演示与测试确定性
