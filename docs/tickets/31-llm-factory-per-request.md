# 31: LLM 工厂 + 按请求解析

**What to build:** 把 LLM 从**全局单例**改为**按请求解析**——请求开始时按发起用户解析出该用哪个 LLM（用户的配置或服务端全局），这是 BYOK 的架构基础。

**Blocked by:** None (can start immediately).

**Status:** ready-for-agent

- [ ] 请求按**发起用户**解析出所用的 LLM；工厂**从外部注入**（stub 可确定性测试，无网络）
- [ ] 用户**未配置**时**回落到服务端全局**配置，行为**与今天完全一致**
- [ ] 一个用户的配置**不影响**另一个用户（隔离）
- [ ] 既有 API 契约与既有测试全绿
