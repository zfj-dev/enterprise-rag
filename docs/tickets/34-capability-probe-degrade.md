# 34: 能力探测 + 代理降级

**What to build:** 探测用户所选模型的**上下文窗口**与**是否支持 tool-calling**，供代理链路判断能否使用；不支持工具时**优雅降级为单步回答**，而不是当场报错。

**Blocked by:** 11 代理循环（ReAct）；31 LLM 工厂 + 按请求解析

**Status:** ready-for-agent

- [ ] 探测**上下文窗口**与 **tool-calling 支持**；结果**缓存**（用 stub 探测器断言调用次数）
- [ ] 探测**失败**时按**保守默认**处理并**标注**，**不得默认"支持工具"**
- [ ] 模型不支持 tool-calling 时，代理**降级为单步回答**且**不抛错**，并说明原因
- [ ] 支持工具时代理走正常多步路径（回归）
