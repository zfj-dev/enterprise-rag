# 21: 工具结果清理（引用后收缩）

**What to build:** 代理循环里，**已被引用过**的工具结果收缩为元信息，**尚未被引用**的保留到本轮结束——多步推理不再把上下文预算耗在已经用掉的内容上。

**Blocked by:** 11 代理循环（ReAct）；17 上下文预算装配 + token 计数缝

**Status:** ready-for-agent

- [ ] 已被引用的工具结果收缩后**仍保留 chunk_id / 文档名 / 页码 / 首部片段**（引用仍可回溯到原文片段）
- [ ] 尚未被引用的结果**保留到本轮结束**，不被提前收缩
- [ ] 收缩只丢"全文"，**不得丢到无法回溯**
- [ ] 收缩后代理仍能完成多步推理（回归：不因收缩而中断）
