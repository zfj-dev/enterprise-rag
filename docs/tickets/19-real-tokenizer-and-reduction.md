# 19: 真实分词器接入 + 降幅可报

**What to build:** 用**与生成模型同族的真实分词器**计数，并报告压缩前后的 token 与**降幅**——让"省了多少"从一句嘴说变成可核对的数字。

**Blocked by:** 17 上下文预算装配 + token 计数缝

**Status:** ready-for-agent

- [ ] 报告给出**压缩前 token / 压缩后 token / 降幅**
- [ ] 分词器惰性加载并缓存；**不可用时该指标标记为不可用**，**不得静默回退到字数估算**
- [ ] 报告写明 token 口径（用的是哪个分词器）
- [ ] 降幅与事实命中**并列**呈现（不单列降幅）
