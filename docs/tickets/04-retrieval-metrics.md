# 04: 检索层指标并入报告

**What to build:** hit@k / recall@k / MRR 与生成层数字出现在**同一份报告**里，并保留混合 / 纯向量 / 纯 BM25 的横向对比，用于定位"检索是否漏了正确答案"。

**Blocked by:** 01 评测核心注入缝

**Status:** ready-for-agent

- [ ] 检索指标经评测核心产出，而非独立脚本单独落盘
- [ ] 报告含各检索方式在多个 k 下的 hit@k / recall@k / MRR
- [ ] **离线可跑**，不依赖运行中的服务
- [ ] 负样本的"向量 top-1 相似度应低于拒答阈值"一并在报告中呈现
