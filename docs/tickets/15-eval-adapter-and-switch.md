# 15: 评测适配器 + 开关

**What to build:** 代理链路能被 **01 的评测核心直接度量**，产出"**事实命中 / 延迟变化**"这两个 ROADMAP 要求的数字；代理由开关控制，**默认关闭时行为与今天完全一致**。

**Blocked by:** 01 评测核心注入缝；14 代理答案接引用 + 1 步自检

**Status:** ready-for-agent

- [ ] 代理以与现有同步问答**同形**的"问题 → 答案 + 来源"被注入评测核心，**评测代码零改动**
- [ ] 同一黄金集可分别跑确定性链路与代理链路，给出事实命中与延迟的**对比**
- [ ] 开关默认**关**；关闭时既有 API 契约与行为不变（现有测试全绿）
- [ ] 演示模式（Fake）下代理不崩、退化为单步回答
