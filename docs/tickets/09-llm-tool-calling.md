# 09: LLM 带工具对话能力（预重构）

**What to build:** LLM 抽象**新增**一条"带工具对话"能力（返回内容 + 工具调用列表），而**既有文本流接口与现有问答链路行为完全不变**。这是让代理能选工具的前置能力。

**Blocked by:** None (can start immediately).

**Status:** ready-for-agent

- [ ] 带工具对话可返回 `{内容, 工具调用列表}`
- [ ] 既有文本流接口的签名与行为不变，**现有测试全绿**
- [ ] 默认（Fake）实现返回**空工具调用**，使依赖方自然退化为单步回答
- [ ] 真实 provider 不支持工具时不炸（明确降级而非抛穿）
