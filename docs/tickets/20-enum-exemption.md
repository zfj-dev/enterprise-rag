# 20: 枚举意图豁免压缩

**What to build:** "列出所有表格 / 有哪些图片 / 列出公式"这类**枚举查询**所注入的大批块**完全不参与压缩**——否则列全会漏项。

**Blocked by:** 17 上下文预算装配 + token 计数缝

**Status:** ready-for-agent

- [ ] 命中枚举意图的上下文**完全不压缩**（回归：列全表格不因压缩漏项）
- [ ] 豁免不因预算大小而失效（即使超预算也豁免）
- [ ] 与既有枚举意图检测复用同一判据，不另起一套
- [ ] 非枚举查询仍正常压缩（豁免不外溢）
