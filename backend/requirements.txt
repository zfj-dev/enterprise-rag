# 核心依赖（起服务/跑测试/基础格式解析所需）
#
# ⚠️ 这些是**下限**，不是锁。对外部署建议用 `pip freeze` 产出一份 lock 再装
# （安全审查 F8：只有下限时，清单管不住某天被装回旧版）。
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
sqlalchemy>=2.0
httpx>=0.27
PyJWT>=2.8
cryptography>=42  # BYOK：凭据加密落库（票 32）
rank-bm25>=0.2.2
# 下限提到 0.0.18：0.0.9~0.0.17 受 CVE-2024-53981（畸形 multipart boundary 导致 DoS）影响
python-multipart>=0.0.18

# 文档解析（常见格式，无 GPU 依赖）
pymupdf>=1.24
python-docx>=1.1
openpyxl>=3.1

# 测试
pytest>=8.0
pytest-cov>=5.0

python-pptx>=1.0  # PPT 解析
