# 企业智能文档问答系统 · 个人版优化报告（v1.0 → v1.1）

> 说明：本文基于对 RAGFlow、FastGPT、Dify、QAnything、AnythingLLM 等同类优秀开源项目的调研，以及大量"单人 RAG 落地踩坑"的经验复盘，对比你的 v1.0「个人实现简化版」与 v2.0「企业级版」，给出把个人版打磨成「单人真能做完、真能上线、真有人用」的具体方案。

---

## ⚙️ 已锁定决定（2026-08-27）

> - **部署形态（W6）= A（本机自用 / 面试演示）+ B（局域网给身边人用）**
> - **LLM 路线 = 混合**：本地跑 bge 嵌入/重排（GPU）+ LLM 生成走**云端 API**（无 GPU）
> - **硬件 = RTX 4050 笔记本（6GB 显存）**
>
> 由此确定的架构、硬件预算、网络要求见 §4；排期见 §5。后续所有决策以本决定为准。

---

## 0. 一句话结论

**你的 v1.0 不缺技术，缺的是两样东西：**

1. **把真正的质量瓶颈（文档解析）做深** —— 同类头部项目 RAGFlow 的招牌就是"深度文档理解"，而踩坑复盘反复证明"模型不是瓶颈，数据才是"。v1.0 用裸 PyMuPDF，恰好落在最弱的一环。
2. **把"能给人用"的几根支柱补上**（引用严谨、用户反馈、权限校验、调试/可观测），**同时砍掉拖后腿的过重基建**（Celery、Milvus 全家桶、以及 v2.0 里那些旗舰名词）。

**v2.0 的方向不是 v1.0 的目标。** v2.0 是 7 人团队、10 周、每月 ¥2.4 万的产物；让单人照搬它的名词去堆，就是踩坑贴里"80% 过度设计"和"GraphRAG 用错了场景"的典型翻车。

---

## 1. 同类优秀项目调研：它们到底强在哪

| 项目 | 定位 | 真正强的地方 | 对单人可借鉴的点 |
|---|---|---|---|
| **RAGFlow**（InfiniFlow，~78k star） | 深度文档理解的 RAG 引擎 | **DeepDoc 文档解析**：OCR、表格结构识别(TSR)、版面布局识别(DLR)，输出带版面元数据的结构化内容；按文档类型分模板解析（通用/论文/书籍/问答/表格/简历）；父子块、RAPTOR、大 PDF 懒加载 | 文档解析是 RAG 质量的天花板；从 PyMuPDF 裸提取 → 结构化解析的升级是对的 |
| **FastGPT**（Labring/Sealos，~29k star） | 知识库问答平台 | 模块化解耦（向量检索/重排/Prompt/模型调用各自独立优化）；三层知识库结构；检索先做"问题优化"（指代消解/查询改写）再检索；引用按 token 而非 top-k 截断 | 检索前加"问题优化"是低成本高收益；模块化解耦方便逐个调优；**部署最轻（2C8G，一条 docker 命令）** |
| **QAnything**（网易有道） | 生产级问答 | **两阶段检索**：BCE-embedding 粗召回 → BCE-reranker 精排；BCEmbedding 双语 SOTA；≤16GB 显存可跑整套（7B 模型+embedding+rerank+OCR）；模块化可换解析/向量/重排 | 双塔检索是标准动作；能用小显存跑全套，说明**单人本地可行** |
| **Dify**（langgenius） | LLM 应用平台 | 数据集/文档/分块三层模型；**多租户隔离是核心边界**（tenant_id 服务端注入，凡涉及权限一律在检索时过滤而非事后）；向量记录必须保留 tenant/kb/doc/version/parent_id 等元数据；embedding 契约一致，换模型必须重建索引 | 权限在**检索时**过滤（不是答案生成后)；元数据字段必须齐全——这决定"能不能给人用" |
| **AnythingLLM**（Mintplex，MIT） | 本地一体机 | 桌面版(单机单用户) / Docker 版(多用户+角色+可嵌入组件)；**一个容器打包 SQLite+LanceDB**，单机单实例、不可横向扩展；Workspace=隔离的文档集合 | **"单容器、自带存储、单机不可扩展"恰恰是单人落地的范本**——别学它做多容器编排 |

> **五家共同点（= 单人真正可落地的骨架）：** ① 混合检索(向量+BM25)+RRF+重排 是标配；② 分块按文档类型分化；③ 引用/溯源必须可靠；④ 检索元数据齐全（含权限）；⑤ 部署尽可能单机轻量。

---

## 2. 单人 RAG 落地踩坑复盘（这份最该听）

网上大量"单人/小团队 RAG 翻车"复盘，结论高度一致，几乎每条都命中 v1.0 的风险点：

- **先做产品再对需求 → 白做。** 有人花了 3 周做混合检索+异步+分级 API，结果用户"根本不想要搜索框，想要推送"。对应你：**别先堆 v2.0 的功能，先让一批真实的人用上你能做的东西。**
- **基建过度设计。** 某位作者原话："Celery 很棒，但那是你有规模问题的时候。**我只有一个笔记本和一个梦想。**" 他自评用 Celery+Redis+3 个 Docker 服务是 **80% 过度**，实际用 `APScheduler + Postgres 任务表` 就能省 400 行代码和一周时间。对应 v1.0 的 Celery+Redis broker。
- **框架依赖地狱。** 有人为了包一个**单次的 HTTP 调用**，拖进整棵 LlamaIndex 依赖树；Chroma 在 `import` 时就加载 onnxruntime，一个 DLL 坏了整个应用启动即崩。对应：你的核心链路上 PyMilvus 就够，别为"编排"引入重框架。
- **数据卫生 >> 管线打磨。** 一位作者坦言"我花两周做管线，零时间做数据，活该"；30 个坏例里只有 **2 个（7%）** 是 LLM 生成问题，其余全是表格拆分、指代消解、近义词缺口、索引过期——全是工程/数据问题。**模型不是瓶颈，数据永远是瓶颈。**
- **没有评估=蒙眼飞行。** 他最后才建评估集（本应该第一个建）：100–200 个真实问题+期望答案，约 6 小时工作量。他还发现 **引用准确率低到 22%**（四分之三的引用撑不起它标注的结论）——修引用对"用户觉得好不好用"的提升，比修答案本身更大。
- **阈值瞎设 = 系统变"神经质"。** 把相关性门限设 0.70，结果 21/30 次触发"自查"，**多余消耗 78% 算力，却返回和基础 RAG 一模一样的结果**。降到 0.55 后自查率从 70% 降到 17%。**阈值是你的 embedding 模型/分块大小/领域语料的函数，必须用真实数据校准，不能拍脑袋。**
- **Demo 是谎言。** 周末 Demo 85%，上线后 38%——因为测试问题只针对"6 份干净、新、纯文字"的文档，真实语料是"一滩沼泽"（扫描件、过期 wiki、互相矛盾的 runbook）。真实修复是**重写 chunker + 加后校验 + 建黄金测试集**，三周纯工程把 38% 拉回 76%。

> **给新人的元教训**（那位重建三次的作者）："**我要是先去读别人的复盘，而不是自作聪明，就能更快交付一个更好的系统。别重复发明轮子，你的问题并不独特。**"

---

## 3. v1.0 逐项对比 + 优化动作

用"该做什么 / 建议做 / 可砍掉或降级"三档，逐项对照 v1.0。

### 3.1 必须做（P0）—— 这是真正的差距

| 环节 | v1.0 现状 | 同类优等做法 | 你要做的 |
|---|---|---|---|
| **文档解析** | 裸 PyMuPDF / python-docx / openpyxl（只提取文字） | RAGFlow 用 DeepDoc 做 **OCR + 表格结构 + 版面布局**，输出带结构的 Markdown | **这是全项目最值得砸时间的地方。** 上表结构识别 + 扫描件 OCR + 版面/去页眉页脚。工具栈见下。不用一步到位，但至少让"表格""扫描件''多栏 PDF"这几类真实文档不出错 |
| **引用严谨性** | Prompt 里让 LLM"标注来源" | 引擎级"**no source → no claim**"；引用校验(每句论断要有出处,二次 LLM 校验)；引用覆盖率作指标；**引用不能指向用户无权看的文档** | 检索结果带**稳定 chunk ID**；展示**源片段原文**；加引用校验；把`引用覆盖率/置信度`当指标。这一条对"给真实用户用"影响最大 |
| **用户反馈闭环** | 只有个 feedback 表(week5 提了一句) | 点赞/点踩/纠错 = 用户侧"事实真相"，喂回评估和迭代 | 做成**真实闭环**：点踩落库 → 上 FAQ/黄金集 → 每周抽坏例重跑评估。别只是记分 |
| **调试/可观测** | week8 才提 Prometheus/Grafana | 每次请求**全链路 trace**（检索到的 chunk id、相似度分数、prompt、回复、反馈）+ 一个"**把这个问题的检索中间结果给我看**"的调试开关 | 做 per-query trace + 一个 Debug 面板。**这既省你自己排查的几百小时，又是面试里"工程素养"的最大加分项** |

**文档解析工具选型**（供你挑，投入产出比从高到低）：

| 场景 | 推荐 | 说明 |
|---|---|---|
| 原生文字 PDF/Word/Excel（主体） | PyMuPDF / python-docx / openpyxl | 最快，留用 |
| 表格、版面、页眉页脚（提升最大） | **Docling**（IBM）或 **PaddleOCR/PP-StructureV3** | Docling：表格/层级恢复强，输出带结构；PP-StructureV3：中文/表格/扫描件榜首(OmniDocBench 94.5%)，Apache2.0，建议 GPU |
| 复杂公式/学术 | MinerU | 学术最强，**AGPL-3.0 许可证**，个人项目注意 |
| 扫描件 | PaddleOCR | 中文扫描件首选 |

> 落地方案：做一个**解析路由**——先判断文档"性质"，普通文本走 PyMuPDF，复杂结构/扫描件走 Docling/PP-Structure，最后统一输出成 Markdown。这就是 RAGFlow 的套路。

### 3.2 建议做（P1）—— 把"能给人用"做扎实，也是面试深度

| 环节 | v1.0 现状 | 你要做的 |
|---|---|---|
| **分块** | Parent-Child 128/512 | 留用，但**给每个 chunk 前置一段"来源上下文摘要"**（contextual retrieval）。这是公认"简单却常常大幅提升检索准确率"的一招，**比建图谱划算得多** |
| **检索** | 向量+BM25+RRF | 留用。**加"问题优化"一步**（多轮对话指代消解 + 查询改写），参考 FastGPT |
| **重排阈值** | 未写清 | **别拍脑袋设阈值**。用 30–50 条真实查询画出分数分布再定；分块小、模型小，绝对分数会偏低。设对阈值能避免"95% 时间都在调优却没用" |
| **权限（多用户）** | week1 只写了个 JWT TODO | 既然要"供用户使用"，必须做**检索时过滤**：chunk 元数据带 `user/role/kb_id`，向量检索的 `expr` 直接过滤，而不是答案生成后再判断。参考 Dify 的做法（权限/租户一律服务端注入 + 检索时下推） |
| **单机基础设施** | Celery+Redis broker + Milvus(etcd+MinIO) + Postgres + Redis + 前端 | **这是 v1.0 最值得砍的地方**。见第 5 节 |

### 3.3 可砍掉或降级（P2 / CUT）

| 内容 | 判断 | 说明 |
|---|---|---|
| **GraphRAG（Neo4j）** | 单人 **CUT**（留"懂原理+写过扩展预案"即可） | 成本高(每 10 万 chunk 实体抽取/摘要约 $200–600)、无增量、延迟高(1200ms vs 300ms)、维护重。**除非你有明确的"多跳/关系型"失败模式**，否则金蛋不在这 |
| **多模态 RAG（Qwen-VL/CLIP）** | 单人 **CUT** | 是所有 RAG 模式里**运维要求最高**的一种。只有当你的知识"真的有可观一部分是图片/图表"才值得 |
| **Agentic 路由 + MCP 工具调用** | 单人 **CUT** | 调研反复警告 "Agentic RAG 被错用在所有场景"——**95% 的企业 RAG 用确定性 Modular RAG 就够** |
| **语义缓存（GPTCache）** | **降级为可选** | 概念好，但单人成本敏感度低时，一个 Redis 精确命中 + 向量相似度去重就够，不必再引一套 GPTCache |
| **K8s 部署清单** | **保留脚本，别真去搭** | 写一份 Deployment+Service+HPA 清单作为"架构延伸能力"展示即可；本地用 Docker Compose 就够了 |
| **RAGAS 评估面板（Grafana）** | **降级** | 做**评估脚本 + 一张趋势图**即可，不必专门上 Grafana。评估本身必须做，但工具从简 |

---

## 4. 单机落地方案（W6 已锁定：混合 + A/B + RTX 4050）

关键决策：**把"用得起的单机"作为第一约束，把"可扩展性"作为文档里讲得清的升级路径。**

### 4.1 架构（混合：本地嵌入/重排 + 云端 API LLM）

```
前端 (React + Ant Design, SSE 流式)
        │
后端 (FastAPI, 无状态)
        │
  ① 提问: 问题优化 → 混合检索 → 重排 → LLM 生成(引用+反馈)
  ② 上传: 解析路由(PyMuPDF/Docling/PP-Structure, CPU异步) → 分块+摘要 → 向量化 → 入库
        │
GPU(本机4050 6G):  bge-large-zh 嵌入(0.3B) + bge-reranker-large 重排(0.56B)
CPU:                FastAPI + Postgres/pgvector + Redis + 前端 + 解析(异步)
LLM:                云端 API (DeepSeek / SiliconFlow 免费额度) —— 无 GPU
存储:  PostgreSQL(pgvector, 一个系统管关系+向量+BM25) +  Redis(会话/限频/embedding缓存) + 文件系统存原始文件
Infra: Docker Compose 一键起
```

### 4.2 硬件预算（RTX 4050 笔记本，6GB 显存）

| 组件 | 显存 / 算力 | 放哪 | 说明 |
|---|---|---|---|
| 嵌入 bge-large-zh（0.3B） | fp16 ≈ 0.7G | 本机 GPU | 轻松 |
| 重排 bge-reranker-large（0.56B） | fp16 ≈ 1.1G（含激活约 2–3G） | 本机 GPU | 小批并发够，总计约 3–4G，留余量 |
| **本地 LLM（如 Qwen-7B）** | **INT4 也要 6G+** | **不可用** | **6G 显存跑不动像样的本地大模型** → 这就是选"云端 API"的硬原因 |
| 解析（Docling/PP-Structure） | CPU 即可 | 本机 CPU | 喂灌时后台跑，不阻塞问答；硬表格/扫描件慢没关系 |
| LLM 生成 | 0 | **云端 API** | 无 GPU、无维护 |

> **结论：6G 显存放得下"嵌入 + 重排"（小模型），但放不下"能用的本地 LLM"。** 混合恰恰是这台机器的**正解**：本地小模型拿"本地模型部署"的简历分，LLM 走 API 保稳定、能服务身边人。

### 4.3 A+B 部署形态（本机自用/演示 + 局域网给身边人用）

- 后端绑定 **`0.0.0.0`**（不是 `127.0.0.1`），别人才能局域网访问。
- 身边人用 **`http://你的内网IP:端口`** 访问（如 `http://192.168.1.20:8000`）。
- **Windows 防火墙放行该端口**（入站规则）——局域网共享**最常卡住的点**，十有八九是它。
- **至少一个轻量登录**：B = 真实多用户，且要"**检索时权限过滤**"，没有用户概念就无从谈起。哪怕是"账号 + 每个用户能看哪些知识库"。
- B 场景下，**引用溯源、用户反馈、调试面板**这几根支柱**真的被用上**了——身边人提问、点踩，反馈喂回你的评估闭环。
- **规模：身边人 = 几个并发**，单机完全扛得住，所以**轻量基建正好**（APScheduler 代替 Celery、pgvector 一套存储、单 Docker Compose），不必上 Milvus/Celery。

### 4.4 两个关键取舍

**① 任务队列：Celery → 削掉。**
踩坑贴原话是"Celery 是 80% 过度设计"。单人 + 单机、文档量不大时，用 **FastAPI `BackgroundTasks` 或 `APScheduler` + Postgres 任务表** 就够，省掉 Redis broker + worker + 重试/DLQ 三件套，少一个 Docker 服务、少一堆踩坑。**简历上仍能写**："异步文档处理 + 失败重试"，不必点名 Celery。真要给面试官讲，就讲"我评估过 Celery，但单机场景下它带来的复杂度大于收益，所以选了更轻的 APScheduler，并留了换 Celery 的抽象层"——这是非常好的工程判断表达。

**② 向量库：Milvus Standalone → 换成 pgvector（或 Qdrant），Milvus 留作升级路径。**
横向数据（多个调研来源一致）：单机、<1000 万向量、且你已经在跑 Postgres 时，**pgvector 是最务实的默认**（零新增基础设施，一套备份，p95 <60ms）。Milvus 单机版要额外拉 etcd + MinIO 两个服务，是"**比你当前的问题更重的数据库**"；真正需要 Milvus 是 100M+ 向量或需分布式/GPU 索引，而单人本地根本到不了。
- **推荐**：用 pgvector + Postgres 全文检索（BM25）做到"关系+向量+关键词一个库全管"。直接**砍掉至少 2 个 Docker 服务**，本地机器压力骤减。
- **保留 Milvus 的简历叙事**：在代码里做一层**向量存储适配接口**，文档写明"当前单机用 pgvector；规模化后按接口切 Milvus/Qdrant"，并在 README 里附 Milvus 迁移 + 集群扩展预案。**这比硬堆 Milvus 更能体现"你会做工程取舍"。**

---

## 5. 重新排的路线图（把功夫花在刀刃上，按 4050 + 混合 + A/B）

你原版把"评估"放在第 7 周、把"解析"放第 2 周，顺序反了。**踩坑贴的铁律：黄金测试集要第一个建，数据卫生要先做。** 建议重排成 6 周（8 周里省下的时间用来打磨质量）：

| 周 | 主题 | 关键产出 |
|---|---|---|
| **W1** | **先定标尺 + 数据** | 收集 15–30 份**你要服务的真实文档**，建 50–100 条黄金测试集（查询/期望答案/期望来源），给它们做个"解析现状体检"；此时**不写检索代码** |
| **W2** | **脚手架 + 解析路由** | Docker Compose（pgvector+Postgres+Redis+前端）、FastAPI 骨架；实现 PyMuPDF 基础解析 + **Docling/PP-Structure 走通表格/扫描件**，输出 Markdown；**把 embedding/rerank 装到 4050 GPU 上跑通** |
| **W3** | **检索 + 分块** | Parent-Child 分块 + **上下文摘要**；**bge-large-zh 本地嵌入（GPU）** 向量化入库；混合检索(向量+BM25)+RRF；**首个可跑 Demo（能上传、能问、能答）** |
| **W4** | **重排 + 生成 + 引用** | **bge-reranker-large 重排（GPU）**；**用真实数据校准阈值**；LLM 生成**走云端 API**（本地不跑大模型）；**引用校验 + 源片段展示**；SSE 流式 |
| **W5** | **给人用的三根支柱（面向局域网多用户）** | 用户注册/登录 + **检索时权限过滤**；**点赞/点踩/纠错闭环**；**per-query trace + 调试面板**；对话历史 |
| **W6** | **A+B 落地 + 评估打磨 + 包装** | 后端绑 `0.0.0.0` + **防火墙放行** + 局域网 `http://内网IP` 给身边人用；跑 RAGAS(或轻量自评)算 Faithfulness/Context Recall/**引用覆盖率**基线；修 Bad Case；录制演示 GIF、写 README 与架构图；整理**简历 bullet** |

> 记住踩坑贴的忠告：**W4 结束必须跑通"能上传、能问、答案带可靠引用"的 Demo**，之后到 W6 打磨 + 上线局域网给身边人用。**写在简历上的每个数字，都要先真跑出来再写。** 另外**4050 的 6G 显存跑不动本地 LLM**，所以**全文的"LLM 生成"一律走云端 API**，本地只负责嵌入/重排/解析。

---

## 6. 面试怎么讲（诚实 + 有深度）

**要敢"讲清楚你没做的东西"，这比硬吹强。** 面试官最欣赏能诚实界定边界、并能讲清取舍的人：

- **为什么不直接上 v2.0 那套（GraphRAG/多模态/Agentic）？**
  > "我调研了 RAGFlow、FastGPT、Dify、QAnything 的实现和大量的落地复盘，发现单个开发者最不该做的就是用 Agentic/GraphRAG 去堆砌——**95% 的企业查询用确定的 Modular RAG 就够了**。GraphRAG 的成本（每 10 万 chunk 约 $200–600 的实体抽取 + 无增量索引 + 延迟 ~4 倍）和它的收益，在单人场景下不成比例。所以我先做**混合检索 + 上下文摘要 + 重排**，用真实数据量出失败模式，**只有当统计数据显示确实存在多跳/关系型问题，我才往上爬**。"

- **为什么本地只跑嵌入/重排、LLM 走云端 API？**
  > "我的机器是 4050 笔记本，**6G 显存跑得动 bge 嵌入和重排这样的小模型，但跑不动一个能用的本地大模型**。所以我做了**混合**：嵌入/重排放本地（保住'本地模型部署'的能力，延迟低、数据不出机），生成用云端 API（稳定、能服务身边好几个人、不折腾 GPU）。这不是偷懒，是**按硬件约束做出的工程取舍**——我做了一层**模型路由**，以后换本地模型或别的 API 都能切换。"

- **为什么核心链路不用 LangChain，自己实现？**
  > "LangChain 的高阶抽象把检索细节藏起来，生产环境排障困难，版本兼容也有风险。我的高频路径（向量检索、重排、生成）用原生 SDK，**仅在需要跨多个工具的编排时参考 LangGraph 的思想**。这样每个环节的原理我都能讲透，代码也可控。"

- **为什么用 pgvector 而不是 Milvus？**
  > "我评估过 Milvus：它是为亿级/分布式设计的，单机版要额外带 etcd 和 MinIO 两个服务，**对我要处理的文档量属于'比问题更重的数据库'**。但我没有拍脑袋只为自己图省事——我做了**向量存储适配层**，README 里写清了 Milvus/Qdrant 的迁移条件和集群扩展预案，这样规模上来能平滑切换。把 Milvus 硬塞进单机不是工程能力，**做对取舍才是**。"

- **怎么证明系统"能给人用"？**
  > "我用三件事保证：**① 引用校验**——每个论断必须能追溯到带稳定 ID 的原文片段，做不到就明说'据现有资料无法确定'，这是防幻觉的关键；**② 用户反馈闭环**——点踩会进入黄金测试集反哺评估；**③ 全链路 trace**——每个问题的检索中间结果、分数、prompt 都可视化，坏了能立刻定位是解析、检索还是生成的问题。而且**它已经部署在局域网给我身边的一批人用**，收到了真实反馈。"

---

## 7. 一句话总结

把 v1.0 的**基建做轻**（APScheduler、pgvector/Postgres 一套存储、Docker Compose、本地小模型 + 云端 API 混合），把**质量做厚**（文档解析路由、上下文摘要、引用校验、校准阈值），把**给人用的支柱补齐**（检索时权限过滤、反馈闭环、调试面板），把 **v2.0 那些旗舰名词留作"调研+扩展预案"而不是真去实现**，并在 **W6 上线局域网（A+B）给身边人用**。这样它才是一个**单人 6 周能交付、本地能真跑、局域网真有人用、面试又能讲出取舍深度**的作品。

---

## 8. 参考资料

- [RAGFlow 官方仓库与说明](https://github.com/infiniflow/ragflow) / [RAGFlow 0.25 发布说明](https://ragflow.io/blog/ragflow-0.25-ingestion-pipeline-agent-sandbox-and-user-level-memory)
- [FastGPT 官方仓库](https://github.com/labring/FastGPT) / [FastGPT 知识库搜索方案](http://doc.fastgpt.cn/zh-CN/guide/dataset/dataset_engine)
- [QAnything（网易有道）](https://www.solosoft.dev/post/qanything-rag-2026/)
- [Dify 架构与多租户](https://deepwiki.com/langgenius/dify/16-glossary)
- [AnythingLLM Docker 部署文档](https://docs.anythingllm.com/installation-docker/overview)
- [RAG 过度设计踩坑复盘：Celery 80% 过度设计、LlamaIndex 依赖地狱、数据卫生、引用准确率 22%](https://www.novakit.ai/blog/rebuilt-rag-system-three-times) / [Naive RAG 灾难复盘](https://cloud.tencent.cn/developer/article/2672808)
- [GraphRAG vs Vector RAG 成本与复杂度](https://vertexrag.com/blog/graph-rag-vs-microsoft-graphrag) / [2026 RAG 架构模式（"别被新颖性牵着走"）](https://aithinkerlab.com/build-rag-systems-2026-architecture-patterns/)
- [RAG 上线前四大支柱：引用、反馈、权限(检索时过滤)、可观测](https://stacklevel.group/en/blog/governed-rag-rollout-checklist) / [RAG 上线检查清单](https://altersquare.io/rag-implementation-checklist-validate-enterprise-documents/) / [RAG 审计清单](https://activewizards.com/blog/the-rag-audit-checklist-what-to-evaluate-before-declaring-a-retrieval-pipeline-production-ready/)
- [文档解析工具对比：Docling/MinerU/PaddleOCR/Unstructured（OmniDocBench）](https://juejin.cn/post/7648651980890357811) / [PaddleOCR vs MinerU](http://a2a-mcp.org/blog/PaddleOCR-vs-MinerU-vs-RAGFlow-vs-Umi-OCR-fr)
- [pgvector vs Qdrant vs Milvus 单机选型](https://dev.to/truongandev/best-vector-database-2026-pgvector-vs-pinecone-vs-qdrant-vs-milvus-1ji1) / [向量数据库选型指南](https://developer.baidu.com/article/detail.html?id=7177603)
